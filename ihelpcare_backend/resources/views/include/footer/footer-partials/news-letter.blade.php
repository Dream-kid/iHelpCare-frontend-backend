<div class="col-lg-6 text-center text-lg-end">
    <div class="sub-title fw-semibold mb-2">{{ __('home.get_the_newsletter')}}</div>
    <form action="#">
        <div class="input-group">
            <input class="btn input-email" name="email" id="email" type="email"
                   placeholder="{{ __('home.your_email_address')}}" required>
            <button class="btn btn-primary" type="submit">{{ __('home.subscribe')}}</button>
        </div>
    </form>
</div>
